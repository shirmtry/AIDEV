streamlit==1.28.0
requests==2.31.0
beautifulsoup4==4.12.2
python-dotenv==1.0.0

# Tùy chọn: Playwright để làm fallback tìm kiếm khi không có Serper API key
# Sau khi pip install, chạy thêm: playwright install chromium
playwright==1.44.0